export interface SingleSlider{
    _id:string,
    title:string,
    image:string,
    link:string | null,
    eyebrow?: string,
    description?: string,
    ctaLabel?: string,
    ctaLink?: string,
    stats?: string[]
  }
